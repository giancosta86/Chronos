event start {
    x := 1;

    while x <= 4 {
        println "X is now: " + x;
        x := x + 1;
    }

    y := 13;
    while (y > 10) {
        println y + " is the value of Y";
        y := y - 1;
    }

    while (100 < 2) {
        assert false;
    }
}